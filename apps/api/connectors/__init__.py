"""Source connector layer.

Connectors collect and normalize evidence from external sources. The core
Demand Engine only sees the source-agnostic contracts defined in
``connectors.base`` and never learns source-specific HTTP/JSON details.
"""
